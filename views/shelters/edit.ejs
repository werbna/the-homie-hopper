<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<link rel="stylesheet" href="/css/styles.css">
<title>Edit <%= shelter.name %></title>
</head>
<body>
  <%- include('../partials/_navbar') %>

  <div class="container">
    <h1>Edit Shelter</h1>
    <form action="/shelters/<%= shelter._id %>?_method=PUT" method="POST" class="shelter-form">
      <div class="form-group">
        <label for="organization">Organization:</label>
        <input type="text" name="organization" id="organization" value="<%= shelter.organization %>" required>
      </div>

      <div class="form-group">
        <label for="name">Name:</label>
        <input type="text" name="name" id="name" value="<%= shelter.name %>" required>
      </div>

      <div class="form-group">
        <label for="location">Location:</label>
        <input type="text" name="location" id="location" value="<%= shelter.location %>" required>
      </div>

      <div class="form-group">
        <label for="contact">Contact:</label>
        <input type="text" name="contact" id="contact" value="<%= shelter.contact %>" required>
      </div>

      <button type="submit">Update Shelter</button>
    </form>
  </div>
</body>
</html>
