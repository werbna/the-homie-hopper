<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit <%= shelter.name %></title>
</head>
<body>
  <%- include('../partials/_navbar') %>

  <h1>Edit Shelter</h1>
  <form action="/shelters/<%= shelter._id %>?_method=PUT" method="POST">
    <input type="text" name="organization" value="<%= shelter.organization %>" required>
    <input type="text" name="name" value="<%= shelter.name %>" required>
    <input type="text" name="location" value="<%= shelter.location %>" required>
    <input type="text" name="contact" value="<%= shelter.contact %>" required>
    <button type="submit">Update Shelter</button>
  </form>
  
